generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Transaction {
  id        Int      @id @default(autoincrement())
  title     String
  type      String
  category  String
  amount    Float
  createdAt DateTime

  @@map("transactions")
}
